version: '3.8'

services:
  poio-blindbox:
    build: .
    ports:
      - "7001:7001"
    volumes:
      - ./midway-project/webapp.sqlite:/app/webapp.sqlite
      - ./midway-project/public/uploads:/app/public/uploads
    environment:
      - NODE_ENV=production
    restart: unless-stopped 